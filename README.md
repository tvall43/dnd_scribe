wanted a thing for dnd since im bad at notes. asked claude/chatgpt/gemini/qwen3.5 to do it for me. here is result

just throw openai api stt and llm in the fields. only llm has an api key box, my whisper.cpp doesnt use api key so i didnt bother yet.

records constantly, sends chunks to whisper for running transcript. notes pane gets appended to every 10 minutes (configurable) by llm summarization. end of session notes are generated by llm reading notes. that also happens automatically with a configurable timer.
